<!-- ここからサイドバー -->
  <!-- ここにform_forを追加します -->
  <%= form_for(@book)do |f| %>
  <!-- 空白で投稿された場合エラーメッセージを表示させる -->
  <% if @book.errors.any? %>
  <div class="alert alert-danger">

    <h2><%= @book.errors.count %> errors prohibited this obj from being saved:</h2>
      <% @book.errors.full_messages.each do |message| %>
      <%= message %>
      <% end %>
    </div>
    <% end %>
    <% end %>
  <!-- </div> -->
    <!-- エラーメッセージここまで -->
<div class="container">
    <!-- フラッシュが消えても高さが変わらないようにしたい -->
  <!-- notice flash表示 -->
  <% if flash.now[:notice] %>
  <p><%= flash[:notice] %></p>
  <% end %>
  <!-- noticd flashここまで -->
  <div class="row">
    <div class="col-sm-3" style="background-color: yellow;">



      <!-- ログインしていたらサイドバーを表示させる -->
      <% if user_signed_in? %>
<div class="container">
        <div class="row">
          <div class="col-sm-3">
      <!-- ここからプロフィール -->
      <h2>User info</h2>
      <!-- ユーザのプロフィール画像 -->
        <%= attachment_image_tag @user_profire, :profile_image, :fill, 100, 100, class: "pull-left profile-thumb", fallback: "no_image.jpg" %>
        <table class="table table-condensed">
          <tr>
            <th>name</th>
            <td><%= @user_profire.name %></td>
          </tr>

          <tr>
            <th>introduction</th>
            <td><%= @user_profire.introduction %></td>
          </tr>
        </table>
      <!-- 自分がカレントユーザの場合はプロフィール編集ボタン表示 -->
      <% if @user_profire == current_user %>
      <!-- クラス：ボタン、レンチのアイコン -->
      <%= link_to "",edit_user_path(@user_profire), class: "btn btn-default glyphicon glyphicon-wrench col-xs-12" %>
      <% end %>
    </div>
  </div>
</div>
      <!-- ここまでプロフィール -->



      <!-- 新規投稿画面ここから -->
      <div class="container">
        <div class="row">
          <div class="col-sm-3">
            <h2>New book</h2>
            <!-- form_for部分 -->
            <%= form_for(@book, url: '/books') do |f| %>

            <div class="field row">
              <label for="book_title">Title</label>
              <%= f.text_field :title, class:"col-xs-12 book_title" %>
            <br>
          </div>

<div class="actions row">
            <label for="book_opinion">Option</label>
              <%= f.text_area :body, class:"col-xs-12 book_body" %>
            <br>
          </div>

            <input type="submit" name="commit" value="Create book" class="btn btn-primary col-xs-12" data-disable-with="Create book">

            <% end %>
            <% end %>
          </div>
        </div>
    </div>
  </div>
  <body>
      <div class="container">
      <div class="col-sm-9" style="background-color: orange;">
    <!-- 新規投稿画面ここまで -->
    <!-- サイドバーここまで -->